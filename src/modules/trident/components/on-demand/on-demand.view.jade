.on-demand
  ul.list-group
    li.list-group-item(ng-repeat="item in vm.items")
      a(ng-bind="item.name")
      ul.list-group
        li(ng-repeat="video in item.videos" ng-bind="video.name" ng-link="['TridentVideo', {videoId: video.videoId}]" class="list-group-item")
      
  ng-outlet
